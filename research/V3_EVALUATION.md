# Version 3 evaluation

Status: verified
Last verified: 2026-08-31
Sources: `python skill/scripts/self_test.py`; `V3_OPTIMIZATION_PLAN.md`; round-4/5 changes re-measured on 2026-08-31; persistent outputs and restore tests under ignored `backups/`

## Outcome

The candidate satisfies the dual-profile contract without weakening validation. Lite measurably reduces the generated harness; full remains equivalent in scope to v2 while correcting stale routes and report hygiene.

## Generated harness comparison

Re-measured on 2026-08-31 against the current v3.1 tree (after the single-writer invariant and work-rounds rounds):

| Candidate | Files | Total lines | `AGENTS.md` lines | Validation |
|---|---:|---:|---:|---|
| v2.0.1 baseline | 12 | 1,582 | 54 | valid; 0 errors, 2 expected warnings |
| v3 lite | 9 | 1,500 | 41 | valid; 0 errors, 2 expected warnings |
| v3 full | 13 | 1,652 | 44 | valid; 0 errors, 2 expected warnings |

Relative to v2.0.1, lite creates 25% fewer files and about 5% fewer lines. Full adds `.ai/.gitignore`, the concrete profile field, the single-writer check, and the optional work-round log while shortening root guidance by ten lines; its total output stays within ~4% of v2.

The two warnings in every fresh scaffold are intentional:

- `PLACEHOLDER_CONTENT`: product and architecture facts still require project evidence.
- `ARCH_OBSERVATIONAL`: no project-native architecture rule exists before product code establishes a boundary.

## Behavioral evidence

- Empty auto-scaffold recommends lite and prints the zero-signal explanation.
- A synthetic repository with 50 source files recommends full at score 3 or greater.
- Explicit profile selection overrides the audit recommendation.
- Audit and preview output expose the recommendation as `score/threshold`; explicit overrides show which recommendation they replaced.
- Lite omits plans, registry, task boards, their validation checks, and every route that presents them as existing (`ARCHITECTURE.md` included — verified by regression scan).
- Full creates and declares all three branches and enables both checks.
- Both outputs contain no references to the removed `docs/architecture/index.md`, `docs/architecture/decisions/`, or `.agents/skills/README.md` routes.
- Both outputs restore `.ai/.gitignore` with `reports/` and `tmp/`.
- A manifest without `project.harnessProfile` still validates as a v2-compatible manifest.
- An invalid profile value fails with `MANIFEST_CONTRACT`.
- An incomplete full declaration fails; a lite declaration with the complete full shape warns as `PROFILE_DRIFT`.
- A lite landing promoted by re-running scaffold with `full` surfaces `PROFILE_PROMOTION_PENDING` until the manifest and guidance are reconciled.
- The registry enforces the single-writer invariant (`AGENT_MULTIPLE_ACTIVE`): one active session passes, two fail; tilde and backtick fences are both skipped, and an unclosed fence warns (`AGENT_FENCE_UNCLOSED`) instead of silently swallowing later sections.
- Unknown template variables abort before any file is written.
- Reapplying the same profile preserves every generated byte.
- Full regression suite passes and leaves no `__pycache__` residue.

## Verification gates

- Candidate diff reviewed against the exact v2.0.1 archive: complete.
- Whitespace diagnostics: none.
- Unresolved placeholders in generated lite/full outputs: none.
- Global installed skill compared with v2.0.1: unchanged.
- Round-3 archive extracted into an isolated project-local directory and its full self-test passed.
- Publication and global installation remain intentionally deferred until explicit approval.
