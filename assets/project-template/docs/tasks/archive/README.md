# Archived task records

Grooming output from agent task boards: `archive/<YYYY-MM-DD>-<agent-id>.md`,
one file per agent per grooming run, containing the completed items exactly as
they appeared on the board. Files here are historical; do not reopen tasks by
editing them — add a new item to the relevant board instead.
